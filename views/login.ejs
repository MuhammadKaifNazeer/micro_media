<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <h3 class="text-4xl mb-3 font-semibold">Login</h3>
      <form action="/login" method="POST" class="flex flex-col gap-3">
        <input
          class="px-3 py-2 bg-zinc-800 rounded-lg"
          type="emial"
          placeholder="email"
          name="email"
          required
        />
        <input
          class="px-3 py-2 bg-zinc-800 rounded-lg"
          type="password"
          placeholder="password"
          name="password"
          required
        />
        <input
          class="px-3 py-2 bg-green-500 font-bold cursor-pointer rounded-lg"
          type="submit"
          value="Login"
        />
      </form>
      <p class="mt-3 text-lg">Don't have an account? <a href="/" class="text-green-500 font-bold">Register</a></p>
    </div>
  </body>
</html>
